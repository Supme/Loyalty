{% for key, content in result %}
    <div class="content" positionId="{{ loop.index }}">
        {{ content.text|raw }}
    </div>
    <div class="modified">{{ content.time|date('d-m-Y h:m')}}</div>
{% endfor %}

{% if csrf %}<div class="csrf" value="{{ csrf }}"/>{% endif %}
